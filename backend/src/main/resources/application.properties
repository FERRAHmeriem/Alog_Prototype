spring.application.name=chna-backend
server.port=8080

# No base-path: controller is mapped at /api/** directly via @RequestMapping
# WebFlux base-path would conflict with our @RestController annotations
